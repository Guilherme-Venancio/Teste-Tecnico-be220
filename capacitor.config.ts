import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'testeTecnicoBe220',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
